{
  "name": "blue-green-app",
  "version": "1.0.0",
  "description": "Node.js application for Blue-Green Deployment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}